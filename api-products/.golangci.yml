linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gofmt
  disable:

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-use-default: false

run:
  timeout: 5m